name: CI
on: [push, pull_request]

jobs:
    build:
      name: Run Script
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v1
        - name: Script
          run: ./runtests.ps1
          shell: pwsh